{
  "name": "dodentocht-live-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/worker.js && node --test test/*.test.mjs",
    "db:local": "wrangler d1 migrations apply dodentocht-live --local",
    "db:remote": "wrangler d1 migrations apply dodentocht-live --remote",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.122.0"
  }
}
